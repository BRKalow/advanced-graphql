# You must create a Union or Interface here, think about it
# Make sure to name it SearchResult
union SearchResult = DevTask | DesignTask | Project

extend type Query {
  search(name: String!): [SearchResult]!
}
